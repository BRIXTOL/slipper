{% capture return_slip %}

  order_number={{ order_name | remove: '#' }}
  &order_url=https://{{ shop.permanent_domain }}/admin/orders/{{ id }}
  &first_name={{ customer.first_name | strip }}
  &last_name={{ customer.last_name | strip }}
  &street_address={{ shipping_address.street | strip }}
  &city={{ shipping_address.city | strip }}
  &zip={{ shipping_address.zip | strip }}
  &country={{ shipping_address.country | strip }}
  &products=
  {% for line in line_items %}
    {{ line.title }}
    {% unless forloop.last -%},
    {% endunless %}
  {% endfor %}

{% endcapture %}

{% schema %}

{
  "hello": "bar"
};

{% endschema %}
